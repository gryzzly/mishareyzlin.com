~ @page.to_html(self)

= haml :page_meta, :layout => false, :locals => { :page => @page }

-# disqus comments
- if @page.path.include? "notes/"
  %aside.main-comments
    = haml :comments, :layout => false
